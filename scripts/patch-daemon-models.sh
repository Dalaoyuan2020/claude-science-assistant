#!/usr/bin/env bash
set -euo pipefail

echo "Windows port: daemon binary model-menu patching is not supported."
echo "Configure model_aliases and model_list_mode=aliases in config.json or the dashboard."
