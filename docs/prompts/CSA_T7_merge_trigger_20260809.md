# 触发指令 T7 (2026-08-09)

读 `docs/plans/CSA_T7_merge_to_model_roles_20260809.md` 并执行。

这是**合并任务，不是重做**。你之前 T0–T6 做的代码没问题，只是任务单把你指到了没有聚合功能的那条分支上（这是任务单的错）。现在要把那两个 commit 合并到 `codex/csa-v0.1.5-model-roles`。

三条要求：
1. **在 `csa-v0.1.5-model-roles` 工作副本里干活**，不是 v0.1.4 那份。
2. 合并时不要覆盖掉 model-roles 已有的聚合/方案一方案二代码。
3. T7-2 要把「预选 → 确认」也应用到方案切换上，这是吕博士明确要求过的。

台账继续用 `TASKS_PROGRESS.md`，追加 T7-1/T7-2/T7-3。
