# CSA Connect Gateway

This directory contains the locked Linux x64 `csa-connect` binary generated by
`scripts/build-connect-gateway.ps1`. The portable package verifies the SHA-256
recorded in `manifest.json` before installing the binary into the selected WSL
user's managed CSA runtime directory.
